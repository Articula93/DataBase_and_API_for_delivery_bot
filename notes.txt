полььзователи
ид
логин (уникальное)
ппароль
из_админ

CRUD

входная модель как есть тоесть все поля (UserIn)

выходная модель пользователь без праоля (UserOut)

RequestCreateUser
    UserIn

PresponseCreateUser
    UserOut

все респонсы содержат UserOut


реализоать псевдо вход пользователя
в запросе только лгин и пароль в ответе тру или фолc

=======================================================
изменяем все что выше но пароль хешируем 
=======================================================
система авторизаци
модифицировать вход ползователя в ответ помимо тру / фолс возвращать токен(строка из букв и цифр)
токен сгенерированный храним в отдельной тоблицы связка ид юзера токен

все функции ге нужно дейсвтие от имени пользовтеля должны принимать токен как параметр запроса